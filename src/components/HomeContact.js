import React from 'react';

const HomeContact = () => {
    return (
        <section id="contact">
            <div>
                <h1>kontakt</h1>
            </div>
        </section>

    );
};

export default HomeContact;
